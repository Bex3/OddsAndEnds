import java.util.List;

/**
 * Created by bearden-tellez on 12/6/16.
 */

// Your program must return the number of jumps that it needs to perform following this logic. Note that the list may contain the number 0, which mean that you can get stuck at a this position forever. In such cases you must return the number -1.
//The length N of the input list will be in the range [1, 1000].
public class Jump {
//    public static int jump_over_numbers(List<Integer> list) {
//        int counter = 0;
//        int listSize = list.size();
//        int nextNumber;
//        int listNumber = list.get(nextNumber);
//
//            for (counter = 0; counter <= listSize; counter ++) {
//                if (counter == 0) {
//                    int listFirst= list.get(0);
//                    if (listFirst == 0 ){
//                        return -1;
//                    }
//                } else {
//                    nextNumber = list.get(listNumber);
//                }
//            }
//
//        return counter;
        //for loop with storing each number with a counter that increases for each jump return the counter
        //if number = 0 then return -1

//    }
}
